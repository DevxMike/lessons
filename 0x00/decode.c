#include <stdio.h>
#include <string.h>

int str_to_int(const char * buff){
    
}

int main(void){
    char tmp[64 * 7 + 1];
    char word[65];
    char channels[7][64];
    int n, c_len;

    scanf("%s", tmp);
    n = strlen(tmp);
    c_len = n / 7;
    for(int i = 0; i < n; ++i){
        
    }
    printf("%s\n", tmp);
    return 0;
}